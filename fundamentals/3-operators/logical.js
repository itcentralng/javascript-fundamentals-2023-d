// We use them to do logic

// &
// Expects the two sides of the operation to be valid

console.log(true & true)
console.log(true & false)
console.log(1 & 0)

// ||
// expects only one side of the operation to be valid
console.log(true || false)
console.log(1 || 0)